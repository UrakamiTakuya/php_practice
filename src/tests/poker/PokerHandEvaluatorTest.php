<?php

use PHPUnit\Framework\TestCase;

require_once(__DIR__ . '/../../lib/PHP/poker/PokerHandEvaluator.php');

class PokerGameTest extends TestCase
{
    public function testStart()
    {
        // $hand = new PokerHandEvaluator(['CA', 'DA']);
        // $this->assertSame(['pair'], $hand->handEvaluator());
    }
}